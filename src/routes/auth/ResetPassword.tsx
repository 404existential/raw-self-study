import {FormEvent,useState} from "react";import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../../hooks/useAuth";
export default function ResetPassword(){const {updatePassword}=useAuth();const navigate=useNavigate();const [password,setPassword]=useState("");const [busy,setBusy]=useState(false);const [error,setError]=useState<string|null>(null);async function submit(e:FormEvent){e.preventDefault();if(password.length<10)return setError("Use at least 10 characters.");setBusy(true);setError(null);const r=await updatePassword(password);setBusy(false);if(r.error)setError(r.error);else navigate("/app",{replace:true})}return <main className="auth-page"><Link to="/" className="brand">RAW</Link><section className="auth-panel"><p className="eyebrow">RAW / PASSWORD</p><h1 className="display text-4xl mt-4">Choose a new password.</h1><form onSubmit={submit} className="mt-8 space-y-5"><label className="field"><span>New password</span><input required minLength={10} type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button w-full" disabled={busy}>{busy?"Saving…":"Save password"}</button></form></section></main>}
