import {FormEvent,useState} from "react";import {Link} from "react-router-dom";import {useAuth} from "../../hooks/useAuth";
export default function ForgotPassword(){const {requestPasswordReset}=useAuth();const [email,setEmail]=useState("");const [sent,setSent]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState<string|null>(null);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError(null);const r=await requestPasswordReset(email);setBusy(false);if(r.error)setError(r.error);else setSent(true)}return <main className="auth-page"><Link to="/" className="brand">RAW</Link><section className="auth-panel"><p className="eyebrow">RAW / RECOVERY</p><h1 className="display text-4xl mt-4">Reset access.</h1>{sent?<p className="muted mt-6">If an account exists for that email, a password-reset message has been requested.</p>:<form onSubmit={submit} className="mt-8 space-y-5"><label className="field"><span>Email</span><input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button w-full" disabled={busy}>{busy?"Sending…":"Send reset link"}</button></form>}<p className="mt-6 text-sm"><Link to="/login" className="link">Back to login</Link></p></section></main>}
