import { FormEvent,useState } from "react";import {Link,useNavigate} from "react-router-dom";import {useAuth} from "../../hooks/useAuth";
export default function Login(){const {signIn}=useAuth();const navigate=useNavigate();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState<string|null>(null);const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError(null);const r=await signIn(email,password);setBusy(false);if(r.error)setError("Email or password could not be verified.");else navigate("/app",{replace:true});}return <main className="auth-page"><Link to="/" className="brand">RAW</Link><section className="auth-panel"><p className="eyebrow">RAW / LOGIN</p><h1 className="display text-5xl mt-4">Back to work.</h1><p className="muted mt-4">Email and password only. No OTP.</p><form onSubmit={submit} className="mt-10 space-y-5"><label className="field"><span>Email</span><input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label className="field"><span>Password</span><input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="error" role="alert">{error}</p>}<button className="button w-full" disabled={busy}>{busy?"Signing in…":"Log in"}</button></form><div className="mt-6 flex justify-between text-sm muted"><Link to="/forgot-password" className="link">Forgot password?</Link><Link to="/signup" className="link">Create account</Link></div></section></main>}
